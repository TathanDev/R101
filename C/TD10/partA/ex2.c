/**
 * La difference entre l’allocation statique et l’allocation dynamique de la mémoire en C est 
 * que la l'allocation statique est effectuée automatiquement par le programme.
 * La mèmoire statique est plus rapide d'accès mais n'est pas très grande.
 * Tandis que l'allocation dynamique est effectuée manuellement et est plus grande mais plus lente.
 */ 